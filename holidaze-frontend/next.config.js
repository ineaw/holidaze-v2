/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = nextConfig

module.exports = {
  images: {
    loader: "default",
    domains: ["res.cloudinary.com"],
  },
};
